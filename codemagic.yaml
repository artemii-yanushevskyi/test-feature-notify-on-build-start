workflows:
  build:
    name: Node.js and npm
    instance_type: windows_test
    environment:
      node: 12
      npm: 7
    scripts:
      - node -v
      - npm -v
      - npm install -g yarn
      - yarn -v

